# 042-retry-webhook-delivery - 2026-07-25-1430

**Ticket:** [042-retry-webhook-delivery](../../../projects/driftnote-sync/tickets/042-retry-webhook-delivery.md)

## Did
- Reproduced the bug: `WebhookDelivery#send` in `lib/webhook_delivery.rb` fired one POST and discarded the response entirely on non-2xx — no retry, no logging, so a single flaky customer endpoint silently ate the notification.
- Added exponential backoff retry: 3 attempts total at 1s/4s/16s delays, using the existing `Retryable` helper already used elsewhere in the codebase (`lib/retryable.rb`) rather than adding a new dependency.
- On final failure after 3 attempts, now logs a structured `webhook_delivery_exhausted` event (customer id, endpoint, last status code) instead of swallowing it — gives support something to grep for.
- Added tests in `spec/webhook_delivery_spec.rb`: one for succeed-on-second-attempt, one for exhaust-all-three-and-log.
- Ran full suite (`bundle exec rspec`) — green, no regressions.
- Opened draft PR: [driftnote-sync#118](https://github.com/lumen-craft/driftnote-sync/pull/118). Kept it to one file plus tests per the Captain's small-PR preference in `captain.md`.

## Left off
Implementation and tests are done and passing. PR is open as a draft. Nothing further to do on this ticket until the Captain reviews — this isn't a "next crew picks up" handoff, it's a "waiting on human" handoff.

## Next steps
1. Captain reviews PR #118 and either merges or requests changes.
2. If changes requested: next watch reads this log, reads the PR review comments, and addresses them on the same branch.
3. If merged: Mate moves this ticket to Done and archives it in the next housekeeping pass.

## Handoff confidence
5 - Clean stopping point. Nothing in flight, nothing ambiguous, single clear next action (Captain review) that isn't crew's to do.

## Notes (optional)
Noticed while in `lib/webhook_delivery.rb` that the same file also handles the *incoming* webhook verification path, which has a separate, unrelated TODO comment about signature validation. Did not touch it — out of scope for this ticket. Worth a future ticket if the Captain wants it.
